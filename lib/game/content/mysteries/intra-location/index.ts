import { Mystery } from "../../../classes";

/**
 * Where Intra is, and what is above it. Frida and Greg already carry most of
 * the breadcrumbs — the year, the second AI, the name "Sentra", a panel near
 * the Reflection Chamber — but they are written into those characters rather
 * than into this mystery, so nothing connects them and nothing can be solved.
 *
 * Unbuilt, like ../your-age, and the same merge candidate.
 */
export const intraLocation = new Mystery({
  id: "intraLocation",
  name: "Where is Intra?",
});
